---
// src/components/HowItWorks.astro

const steps = [
  {
    step: "01",
    title: "Regístrate",
    description:
      "Crea tu cuenta gratuita en segundos. No requerimos tarjeta de crédito para empezar con el plan Starter.",
  },
  {
    step: "02",
    title: "Carga tu Stock",
    description:
      "Importa tus productos masivamente desde Excel o carga tus productos manualmente.",
  },
  {
    step: "03",
    title: "Toma el Control",
    description:
      "Visualiza tus métricas en tiempo real, configura alertas y automatiza tus pedidos a proveedores.",
  },
];
---

<section
  id="how-it-works"
  class="w-full bg-slate-50 dark:bg-slate-900/50 py-24 px-4 sm:px-10 border-b border-gray-100 dark:border-gray-800"
>
  <div class="max-w-[1280px] w-full mx-auto">
    <div class="text-center mb-16 md:mb-24">
      <h2
        class="text-slate-900 dark:text-white text-3xl sm:text-4xl font-extrabold leading-tight mb-4"
      >
        Puesta en marcha <span class="text-brand-blue">simplificada</span>
      </h2>
      <p
        class="text-slate-muted dark:text-gray-400 text-lg font-medium max-w-2xl mx-auto"
      >
        Olvídate de implementaciones complejas. Nuestro sistema está diseñado
        para que estés operativo en menos de 15 minutos.
      </p>
    </div>

    <div class="relative grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-8">
      <div
        class="hidden md:block absolute top-[2.5rem] left-[16%] right-[16%] h-0.5 bg-gradient-to-r from-slate-200 via-brand-blue/50 to-slate-200 dark:from-slate-700 dark:via-brand-blue/50 dark:to-slate-700 -z-0"
      >
      </div>

      {
        steps.map((item) => (
          <div class="relative flex flex-col items-center text-center z-10 group">
            <div class="w-20 h-20 rounded-full bg-white dark:bg-slate-800 border-4 border-slate-50 dark:border-slate-700 flex items-center justify-center mb-6 shadow-lg group-hover:scale-110 group-hover:border-brand-blue/30 transition-all duration-300">
              <span class="text-2xl font-black text-brand-dark dark:text-white group-hover:text-brand-blue transition-colors">
                {item.step}
              </span>
            </div>

            <h3 class="text-slate-900 dark:text-white text-xl font-bold mb-3">
              {item.title}
            </h3>
            <p class="text-slate-muted dark:text-gray-400 text-base leading-relaxed max-w-xs">
              {item.description}
            </p>
          </div>
        ))
      }
    </div>

    <div class="mt-16 text-center">
      <button
        class="inline-flex items-center justify-center gap-2 px-8 py-4 rounded-full bg-brand-dark text-white font-bold hover:bg-slate-700 transition-all shadow-lg hover:shadow-brand-dark/20 hover:-translate-y-1"
      >
        Comenzar Ahora
        <span class="material-symbols-outlined text-sm">arrow_forward</span>
      </button>
    </div>
  </div>
</section>
